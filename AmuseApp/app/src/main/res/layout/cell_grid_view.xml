<?xml version="1.0" encoding="utf-8"?>
<!-- Block clicks: android:descendantFocusability="blocksDescendants" -->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white"
    android:orientation="vertical"
    android:gravity="bottom">

    <ImageView
        android:id="@+id/recipe_image"
        android:transitionName="@string/transition_detail_image"
        android:contentDescription="@string/image"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop" />

    <RelativeLayout
        android:id="@+id/fade_views"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <View
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/bottom_gradient"/>

        <TextView
            android:id="@+id/recipe_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/gridview_cell_title_left_margin"
            android:layout_marginBottom="@dimen/gridview_cell_title_bottom_margin"
            android:layout_alignParentBottom="true"
            android:maxLines="4"
            android:ellipsize="end"
            android:textColor="@android:color/white"
            android:textStyle="bold"
            android:textSize="@dimen/gridview_cell_title_font_size" />

    </RelativeLayout>
</RelativeLayout>
